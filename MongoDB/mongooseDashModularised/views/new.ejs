<!DOCTYPE html>
<html>
<head>
        <meta charset="utf-8">
        <title>New Mongoose</title>
</head>
<body>
        <h1>New Mongoose</h1>
         <% if(typeof(errors) != 'undefined' ) { %>
                 <% for (var x in errors) { %>
               <h3><%= errors[x].message %></h3>
              <% } %>
          <% } %>

          <form class="mongoos" action="new" method="post">
                  Name: <input type="text" name="name" value="">
                  <button type="submit" name="button">Submit</button>
          </form>

</body>
</html>
