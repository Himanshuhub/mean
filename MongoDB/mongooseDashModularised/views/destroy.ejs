<!DOCTYPE html>
<html>
<head>
        <meta charset="utf-8">
        <title>Destroy Mongoose</title>
</head>
<body>
        <h1>Destroy Mongoose</h1>
         <% if(typeof(errors) != 'undefined' ) { %>
                 <% for (var x in errors) { %>
               <h3><%= errors[x].message %></h3>
              <% } %>
          <% } %>

          <h3>
                  Name: <%= users.name %><br>
          </h3>

          <form class="mongoos" action="/destroy/<%=users._id%>" method="post">
                  <button type="submit" name="button">Remove Mongoose</button>
          </form>

</body>
</html>
