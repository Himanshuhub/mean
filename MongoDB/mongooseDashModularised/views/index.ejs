<!DOCTYPE html>
<html>
<head>
        <meta charset="utf-8">
        <title>Mongoose Dashboard</title>
</head>
<body>
        <h1>Mongoose Dashboard</h1>
         <% if(typeof(errors) != 'undefined' ) { %>
                 <% for (var x in errors) { %>
               <h3><%= errors[x].message %></h3>
              <% } %>
          <% } %>

                  <% for(var i=0; i<users.length; i++){  %>
                  <p>Name: <%= users[i].name %></p>
                  <a href="/update/<%=users[i]._id%>">Update</a>
                  <a href="/destroy/<%=users[i]._id%>">Delete</a>
                  <% }%>

</body>
</html>
