<!DOCTYPE html>
<html>
<head>
        <meta charset="utf-8">
        <title>Quotes</title>
</head>
<body>
        <h1>ID</h1>
         <% if(typeof(errors) != 'undefined' ) { %>
                 <% for (var x in errors) { %>
               <h3><%= errors[x].message %></h3>
              <% } %>
          <% } %>

          <h3>
                  <% for(var i=0; i<users.length; i++){  %>
                  Name: <%= users[i].name %>
                  <% }%>
          </h3>


</body>
</html>
