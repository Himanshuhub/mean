<!DOCTYPE html>
<html>
<head>
        <meta charset="utf-8">
        <title>Quotes</title>
</head>
<body>
        <h1>Here are the awesome quotes!</h1>
         <% if(typeof(errors) != 'undefined' ) { %>
                 <% for (var x in errors) { %>
               <h3><%= errors[x].message %></h3>
              <% } %>
          <% } %>

        <h3>
                <% for(var i=0; i<users.length; i++){  %>
                Name: <%= users[i].name %>,
                Age: <%= users[i].quote %><br>
                <% }%>
        </h3>

</body>
</html>
